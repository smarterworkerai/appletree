{
  "name": "appletree",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3333",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3333",
    "lint": "node --check src/main.js",
    "test:unit": "python3 -m unittest discover -s mise-helper/tests -p test_*.py"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}
